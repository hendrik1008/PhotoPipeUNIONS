#/bin/tcsh
# 2 arguments: input and output filename
# replace 5th column by its double, turns fluxradius into fwhm
# any line starting with # is passed through.

awk '{if (substr($0,0,1)=="#") print($0); else print($1,$2,$3,$4,2*$5,$6,$7,$8,$9,$10)}' <$1 > $2

